<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Productos</title>
</head>
<body>
    <h1>Productos</h1>
    <% if(products.length > 0) {%>
        <% products.forEach(p => {%>
            <li><%= p.title %></li>
        <% })%>  
        <button type="submit">Cargar otro producto</button>
    <% }else {%>
        <p>No hay productos</p>
        <button type="submit">Cargar un producto</button>
    <% }%>
    
        <!-- <p>No hay productos</p>
        <button type="submit">Cargar un producto</button> -->
    
    
</body>
</html>